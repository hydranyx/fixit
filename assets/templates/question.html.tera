{% extends "ask" %}

{% block question %}
    <h3>{{question}}</h3>
    <form>
    {% for s in answers %}
        <a href="/hello/{{s}}" class="pure-button pure-button-primary">{{s}}</a>
    {% endfor %}
    </form>
{% endblock question %}
